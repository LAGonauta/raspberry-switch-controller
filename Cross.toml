[target.aarch64-unknown-linux-gnu]
pre-build = [
    "dpkg --add-architecture $CROSS_DEB_ARCH",
    "apt-get update && apt-get --assume-yes install libclang-dev libudev-dev:$CROSS_DEB_ARCH pkg-config:$CROSS_DEB_ARCH libbluetooth-dev:$CROSS_DEB_ARCH"
]